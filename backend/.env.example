# Copy this file to .env and replace the development values before deployment.
DATABASE_URL=sqlite:///./comunidade.db
# Para usar o Supabase Postgres, informe a connection string aqui (ou em SUPABASE_DATABASE_URL).
SUPABASE_DATABASE_URL=
SUPABASE_JWT_SECRET=
UPLOAD_DIR=./uploads
SECRET_KEY=replace-with-a-long-random-secret
# Generate this with: python -m app.scripts.hash_password
ADMIN_PASSWORD_HASH=$argon2id$v=19$m=65536,t=3,p=4$REPLACE_ME
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
COOKIE_SECURE=false
